/**
 * Options页面组件导出
 * 专门用于Options界面的组件
 */

// Options页面组件（如果有的话）
// 目前Options页面使用相同的组件，但可以在这里添加Options特有的组件

export { default as SettingsPage } from '../SettingsPage';
export { default as ChromeSyncSettings } from '../ChromeSyncSettings';

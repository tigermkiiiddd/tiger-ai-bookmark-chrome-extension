# TIGERMARKIII 需求说明（体验优先）

| 属性 | 说明 |
|------|------|
| 文档用途 | 定义「做到什么算对」——以**用户可感知的体验**为准绳，供产品验收与研发对齐 |
| 读者 | 产品负责人、设计、研发 |
| 撰写原则 | **禁止**以「技术功能清单」代替需求表述；每条需求必须先写清**达成的体验**与**用户如何感知**；技术实现仅在文末「技术路径」集中注明，避免喧宾夺主 |

**文档版本**：1.1  
**修订日期**：2026-05-20  

---

## 1. 总体目标体验

用户用 TIGERMARKIII 管理 Chrome 书签时，应同时获得两种互补感受：

1. **在浏览器里**：收藏、查找、日常整理**顺手、少打断**，内置智能帮助「快一点理解当前页」。  
2. **在需要深度整理时**：可以请**自己信任的桌面侧智能助手**（例如 Cursor 等）参与：像真人一样**看全局、合并过碎的结构、纠正明显不合理的分类**，而不必为了这件事**每天多一道「先打开某个小软件」的习惯负担**。

本需求文档后续条目，均服务于上述总体体验；**不以「接入了某协议、某 API」作为验收口径**。

---

## 2. 需求条目（体验定义与验收口径）

### REQ-01 深度整理：从「一次猜完」到「像人一样看全局」

**要达到的体验**

- 用户感觉：智能助手在整理书签时，**会看整体**，而不是只对单条书签「猜一个就落库」。  
- 用户能解释得清：为什么这次整理后，**文件夹不会越拆越碎、标签不会满天飞**——助手的行为符合「合并、归类、少而稳」的预期。  
- 当收藏量很大时，用户仍觉得：**有人帮我把结构捋顺了**，而不是多了一堆更难维护的细分类。

**用户如何感知「需求被满足」**

- 用户愿意在「结构乱、历史遗留多」时，**主动选用外部助手整理**，且整理完成后**主观负担下降**（找得到、分得粗、改得动）。  
- 用户**不依赖**「反复点内置整理直到碰巧满意」作为唯一出路。

---

### REQ-02 启动与日常负担：「我只管开 Chrome」

**要达到的体验**

- 用户**不需要**形成固定习惯：「每次上网前先双击启动某某辅助程序」。  
- 用户正常**打开 Chrome、使用扩展**时，为「外部助手参与整理」所必需的本地配合，**已经就绪或在使用瞬间自动就绪**；用户**不把**它当成第二个要伺候的「主产品」。  
- 用户关掉浏览器后，**不期待**还要手动去关一串为整理书签而挂着的后台窗口（除非产品明确告知需保留的特殊场景）。

**用户如何感知「需求被满足」**

- 访谈或可用性反馈中，用户**不**将「整理书签」与「先开一个小工具」绑定。  
- 支持人员**很少**收到「我忘了先开某某所以助手连不上」类问题。

---

### REQ-03 双路径清晰：「快整理」和「稳整理」各干各的

**要达到的体验**

- 用户在扩展里用内置能力时，心理预期是：**快、就地、适合当前页**。  
- 用户请外部助手时，心理预期是：**慢一步可以接受，但要更稳、更全局**。  
- 用户**能理解**两条路径各自适合什么场景，而**不**觉得「两个神仙打架、书签忽来忽去」。

**用户如何感知「需求被满足」**

- 界面或引导文案能让用户在**30 秒内**说对：什么时候用内置、什么时候用助手。  
- 同一时期两条路径混用时，用户**几乎不遇到**「刚整理完又被打乱」的失控感（见 REQ-05）。

---

### REQ-04 可控与可恢复：「敢让助手大改」

**要达到的体验**

- 当助手建议**大批量**移动分类、合并标签时，用户有机会在**落库前**看到「将要发生什么」，并选择**继续或打住**。  
- 一旦发生「分太细 / 合错 / 一口气改太多」等情况，用户有**可理解的后悔药**（例如明确的撤销边界或回滚点），而不是只能徒手一条条改回去。

**用户如何感知「需求被满足」**

- 用户敢用外部助手做**结构性**调整，而不是只敢用来打标签。  
- 误操作后的恢复成本，在用户的**心理可承受范围**内（由产品通过可用性测试界定基线）。

---

### REQ-05 并发与冲突：「不会两个整理源互相踩脚」

**要达到的体验**

- 用户在同一时段既用内置整理、又用外部助手时，**不会**感觉书签在「两个人各拉各的」导致结果不可预测。  
- 最差情况下，用户也能得到**明确提示**：现在谁在改、要不要等一等，而不是静默坏数据。

**用户如何感知「需求被满足」**

- 冲突场景下用户**能说出**发生了什么、下一步该怎么做；支持工单中「数据莫名其妙变了」的比例可控。

---

### REQ-06 能力边界：用户心智里「这就是书签管家」

**要达到的体验**

- 用户清楚：这个能力是用来**整理、检索、维护书签**的，**不是**「远程操控整台浏览器、在任意网页里执行脚本」的那类产品。  
- 用户不会因为营销或界面暗示，误以为可以拿它做**与书签无关**的高风险操作。

**用户如何感知「需求被满足」**

- 新用户能在**不读技术文档**的前提下，用一句话描述「它能帮我干什么」。  
- 安全与隐私相关的用户担忧，**集中在书签数据本身**，而非「它是不是还能动我别的」。

---

### REQ-07 助手侧体验：「在我习惯用的工具里就能下令」

**要达到的体验**

- 用户在自己常用的、支持智能助手的桌面工具里，用**自然语言或该工具惯用的方式**下达「整理书签」类意图时，**能驱动**上述全局整理能力，而不必为了整理书签**单独学一套新软件**。  
- 用户感知到：助手**真的在动我的书签结构**，而不是只生成一段「建议文本」让用户自己手抄进扩展。

**用户如何感知「需求被满足」**

- 用户在助手工具内完成一次「从意图到书签结构变化」的闭环后，**愿意复用**该路径处理下一次大整理。

---

### REQ-08 隐私与信任：「我的书留在我的地盘」

**要达到的体验**

- 用户默认假设：书签相关数据**优先留在本机、由自己掌控的范围内**；外部助手参与时，用户知道**数据流大致去哪**（仍在自己设备与已选工具之间），而不是「被悄悄同步到不明云端」。  
- 任何需要用户额外授权或安装配套的场景，**说人话、一次说清**，不埋在小字里。

**用户如何感知「需求被满足」**

- 用户在启用外部整理前，能用自己的话复述：**谁会看到什么、关掉什么就停什么**。

---

### REQ-09 归档批量缩略图：「自动跑完，但我看得见」

**要达到的体验**

- 用户在**归档或同类大批量整理**场景下，可以发起**一次**「为多条书签补缩略图」的操作；之后**不必逐条手动点开网页再截**，由产品**自动按队列处理**多条目标页。  
- 这里的 **「自动」指：少手点、按队列连续执行**；**不是**「静默、无感、用户不知道浏览器在干什么」。用户预期是：**我明确知道系统正在批量截图**，并且**允许**为此出现**前台操作**（例如标签页被依次切到目标页、窗口焦点变化等），只要这些行为在**开始前有说明**、过程中**有清晰反馈**。  
- 批量进行中，用户始终能感知：**当前处理到第几条、还剩多少、成功/失败/跳过各有多少**；并能**暂停或中止**，中止后状态可理解（不会误以为还在后台偷偷跑）。

**用户如何感知「需求被满足」**

- 用户愿意在「归档前统一补图」时走批量流程，而不是认为「只有一条条手动才放心」。  
- 用户**不会**用「是不是在我不知道的时候一直在截屏」来形容该产品——相反，用户描述的是「我让它跑，它当着我面跑完/我中途停了」。  
- 对单条失败（登录墙、空白、超时等），用户能得到**单条层面的结果说明**，且**不影响**其对整批流程「总体可控」的信心。

---

## 3. 非目标（体验口径）

以下体验**不在**本需求文档承诺范围内，避免验收范围膨胀：

- 在任意网页上执行用户脚本、做通用浏览器自动化。  
- 不打开 Chrome 仍能完整管理本产品书签（若未来有云端另议，不在此版本体验承诺内）。  
- 保证某一特定第三方助手品牌「永远兼容」——验收以「用户选定的、符合行业常见接入方式的助手」为准，具体品牌组合可在发布说明中列**已验证组合**。  
- **不在此版本承诺**：对任意 URL 在**完全不打开、不激活、用户毫无感知**的前提下「静默」生成缩略图（与 REQ-09 的「可感知、可前台」取向相反，不作为验收标准）。

---

## 4. 为满足以上需求所采用的技术路径（供研发对齐）

本节**仅回答「用什么工程手段支撑上述体验」**，不替代第 2 节的体验验收；若实现与体验冲突，**以体验条款为准**。

| 体验诉求 | 技术路径（实现约束） |
|----------|----------------------|
| REQ-02：用户不必手开常驻小软件；随 Chrome 使用即就绪 | 扩展在合适时机（如浏览器启动或首次使用整理能力时）通过 **Chrome Native Messaging** 由 **Chrome 自动拉起**本机宿主进程；用户日常**无需**单独双击启动该宿主。宿主生命周期与扩展连接/浏览器退出策略由实现决定，但**产品目标**是「不增加用户每日手动启动负担」。 |
| REQ-07：桌面侧助手（如 Cursor）可驱动整理 | 宿主进程**同时承担**对外的 **MCP（Model Context Protocol）服务能力**，使常见 MCP Client 能调用「书签域」整理相关能力；扩展与宿主之间通过 **Native Messaging** 双向传递整理意图与结果。 |
| REQ-01、REQ-04、REQ-06：全局整理、可控提交、能力边界清晰 | MCP 暴露的能力集合**限定在书签域**（列举、草案变更、受控提交、撤销边界等），**不包含**任意网页脚本执行类能力；具体工具命名与粒度由实现定义，但须可映射回第 2 节体验条款。 |
| REQ-05：双路径不互相踩 | 实现层通过队列、互斥或显式冲突提示等机制满足 REQ-05 的体验描述；具体机制不在本文档展开。 |
| REQ-09：归档批量缩略图，可前台、非静默 | 采用 **`chrome.tabs` 激活目标标签 + `captureVisibleTab` 截取可见区域** 等扩展既有能力组合，按队列自动执行；缩略图由实现侧对截图做缩放/压缩。须遵守浏览器对截图 API 的**频率与性能**约束（实现侧做节流与排队）。**不承诺**在标签页从不激活的前提下完成截图。 |

**说明**：Chrome 扩展自身无法在本地扮演「被外部 MCP Client 直连的监听服务」以替代宿主；故宿主进程是满足 REQ-02 与 REQ-07 **同时成立**的**必要工程组件**，但在产品上应呈现为「安装/启用一次后的无感配套」，而非用户心智中的第二个独立产品。

---

## 5. 修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.0 | 2026-05-20 | 首版：体验优先的需求说明 + 技术路径集中附录 |
| 1.1 | 2026-05-20 | 新增 REQ-09（归档批量缩略图：自动队列、允许前台、非静默）；非目标与第 4 节技术路径对齐 |

---

*验收时以第 2 节「要达到的体验」「用户如何感知」为主；第 4 节仅供实现与架构对齐，不作为用户可见文案来源。*
